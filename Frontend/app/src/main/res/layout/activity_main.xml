<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <View
        android:id="@+id/topView"
        android:layout_width="291dp"
        android:layout_height="154dp"
        android:layout_marginTop="16dp"
        android:background="#FFFDEB"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/bottomView"
        android:layout_width="330dp"
        android:layout_height="390dp"
        android:layout_marginBottom="16dp"
        android:background="#FFFDEB"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.492"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/main_loginButton"
        android:layout_width="246dp"
        android:layout_height="96dp"
        android:layout_marginTop="72dp"
        android:text="Login"
        app:layout_constraintBottom_toTopOf="@+id/main_registrationButton"
        app:layout_constraintEnd_toEndOf="@+id/bottomView"
        app:layout_constraintStart_toStartOf="@+id/bottomView"
        app:layout_constraintTop_toTopOf="@+id/bottomView"
        app:layout_constraintVertical_bias="0.411" />

    <Button
        android:id="@+id/main_registrationButton"
        android:layout_width="246dp"
        android:layout_height="96dp"
        android:layout_marginBottom="92dp"
        android:text="Reegister"
        app:layout_constraintBottom_toBottomOf="@+id/bottomView"
        app:layout_constraintEnd_toEndOf="@+id/bottomView"
        app:layout_constraintStart_toStartOf="@+id/bottomView" />

    <ImageView
        android:id="@+id/main_logoView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@+id/topView"
        app:layout_constraintEnd_toEndOf="@+id/topView"
        app:layout_constraintStart_toStartOf="@+id/topView"
        app:layout_constraintTop_toTopOf="@+id/topView"
        app:srcCompat="@drawable/logotouse" />

    <ImageButton
        android:id="@+id/main_adminButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:drawable/ic_dialog_dialer"
        tools:ignore="SpeakableTextPresentCheck" />

</androidx.constraintlayout.widget.ConstraintLayout>